apiVersion: v1
kind: Service
metadata:
  name: embedding-api # This makes 'http://embedding-api:80/embed' work
spec:
  selector:
    app: embedding-api # Must match the label in your Embedding API deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080 # Or whatever port your API container uses
